pip install -r requirements.txt       (安装依赖)

cp .env.example .env                (配置)

python api_solver.py --browser_type camoufox --thread 5 --debug         (启动 Turnstile Solver)

python grok.py      （运行注册程序）